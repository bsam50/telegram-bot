# بوت إدارة القروبات

## التشغيل
ثبت Python 3.12 أو أحدث ثم:
`pip install -r requirements.txt`

ضع `BOT_TOKEN` و `OWNER_ID` في متغيرات البيئة، ثم:
`python main.py`

لا تضع توكن البوت داخل GitHub.
البوت يحتاج أن يكون مشرفًا في المجموعة مع صلاحيات الحذف والحظر والتقييد.
